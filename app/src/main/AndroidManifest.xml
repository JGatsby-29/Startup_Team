<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.StartUp_Team">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_logo_white"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_logo_white_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ex_fragment">
        <activity
            android:name="com.example.StartUp_Team.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>